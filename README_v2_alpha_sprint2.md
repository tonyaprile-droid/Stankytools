# StankyTools v2 Alpha Sprint 2

## Changes

- Guild members/roles are hidden in Settings until **View Members / Roles** is clicked.
- Member management is now inside the dialog only.
- Catalog importer now crawls the requested pages plus known subcategory listing pages so it catches many items that the main pages do not render initially.
- Import report now includes the expanded listing pages used.

## Notes

The importer still uses polite delays and runs in a background thread.
